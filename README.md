# Doc V2

## Backend

Install dependencies from the virtualenv package list:

```bash
python -m pip install -r backend/requirements.txt
```

Run the backend server with hot reload:

```bash
python backend/main.py
```
