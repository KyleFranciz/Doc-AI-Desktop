# Backend environment for Doc V2
# Copy this file to `.env` and customize before running FastAPI.

# URL that the frontend dev server (Vite/Electron) runs on so CORS is scoped correctly.
FRONTEND_URL=http://localhost:5173

# Local SQLite database path (relative to the backend root).
DB_PATH=./database.db

# Metadata used by Chromadb for persistent storage location.
APP_NAME=DocV2
APP_AUTHOR=YourName
