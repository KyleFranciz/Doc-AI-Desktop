# Frontend environment helpers for Doc V2 (Electron renderer)
# Set this in development to point Electron at the Vite dev server.
VITE_DEV_SERVER_URL=http://localhost:5173

# In production, leave the above empty so Electron falls back to the bundled build.
